/* Create a funciton to calculate the area of rectangle given its width and height. */

function calculatearea(width, height){
    return width * height;
}

let area = calculatearea(5, 10);
console.log("Area of Rectangle is : " + area); // 50